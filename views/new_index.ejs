<% include ./header.ejs %>
<link rel="stylesheet" href="<%=helpers.staticurl('/css/idangerous.swiper.css')%>">
<link rel="stylesheet" href="<%=helpers.staticurl('/css/new_index.css')%>">
<!--<link rel="stylesheet" href="css/index.css">-->
<div class="page-newIndex">
	
	<!-- banner -->
	<div class="swiper-box">
		<div class="swiper-container js-swiper-container">
			<div class="swiper-wrapper" id="swiperwrapper">
				<%if(banner&&banner.length > 0){%>
				<%banner.forEach(function(v){%>
				<div data-description="<%=v.description%>" class="swiper-slide" style="background: url(<%=helpers.drawImage(v.basic_image_1)%>) no-repeat top center;">
					<%if(v.basic_url){%>
						<a target="_blank" href="<%=v.basic_url%>">
							
						</a>
					<%}%>
				</div>
				<%})%>
				<%}%>
			</div>
		</div>
		<div class="pagination js-pagination"></div>
	</div>
	<!-- /banner -->
	
	<div class="title">机器人电脑</div>
	<%if(new_index_jqr&&new_index_jqr.length > 0){%>
	<div class="product product<%=new_index_jqr.length%>">
		<%new_index_jqr.forEach(function(v,i){%>
		<%if(!i){%>
		<div class="l">
			<a href="<%=v.basic_url%>">
				<img class="bg" src="<%=helpers.drawImage(v.basic_image_1)%>" />
				<%if(v.basic_image_2){%>
				<img class="abs" src="<%=helpers.drawImage(v.basic_image_2)%>" />
				<%}%>
			</a>
		</div>
		<ul class="r">
			<%}else{%>

			<li>
				<a href="<%=v.basic_url%>">
					<img class="bg" src="<%=helpers.drawImage(v.basic_image_1)%>" />
					<%if(v.basic_image_2){%>
					<img class="abs" src="<%=helpers.drawImage(v.basic_image_2)%>" />
					<%}%>
				</a>
			</li>
			<%}%>
			<%})%>
		</ul>
	</div>
	<%}%>
	
	<div class="title">科技生活</div>
	<%if(new_index_kj&&new_index_kj.length > 0){%>
	<div class="product product<%=new_index_kj.length%>">
		<%new_index_kj.forEach(function(v,i){%>
		<%if(!i){%>
		<div class="l">
			<a href="<%=v.basic_url%>">
				<img class="bg" src="<%=helpers.drawImage(v.basic_image_1)%>" />
				<%if(v.basic_image_2){%>
				<img class="abs" src="<%=helpers.drawImage(v.basic_image_2)%>" />
				<%}%>
			</a>
		</div>
		<ul class="r">
			<%}else{%>

			<li>
				<a href="<%=v.basic_url%>">
					<img class="bg" src="<%=helpers.drawImage(v.basic_image_1)%>" />
					<%if(v.basic_image_2){%>
					<img class="abs" src="<%=helpers.drawImage(v.basic_image_2)%>" />
					<%}%>
				</a>
			</li>
			<%}%>
			<%})%>
		</ul>
	</div>
	<%}%>
	
	<div class="title">科技玩具</div>
	<%if(new_index_kjwj&&new_index_kjwj.length > 0){%>
	<div class="product product<%=new_index_kjwj.length%>">
		<%new_index_kjwj.forEach(function(v,i){%>
		<%if(!i){%>
		<div class="l">
			<a href="<%=v.basic_url%>">
				<img class="bg" src="<%=helpers.drawImage(v.basic_image_1)%>" />
				<%if(v.basic_image_2){%>
				<img class="abs" src="<%=helpers.drawImage(v.basic_image_2)%>" />
				<%}%>
			</a>
		</div>
		<ul class="r">
			<%}else{%>

			<li>
				<a href="<%=v.basic_url%>">
					<img class="bg" src="<%=helpers.drawImage(v.basic_image_1)%>" />
					<%if(v.basic_image_2){%>
					<img class="abs" src="<%=helpers.drawImage(v.basic_image_2)%>" />
					<%}%>
				</a>
			</li>
			<%}%>
			<%})%>
		</ul>
	</div>
	<%}%>

	<div class="title">周边配件</div>
	<%if(new_index_parts&&new_index_parts.length > 0){%>
	<div class="product product<%=new_index_parts.length%>">
		<%new_index_parts.forEach(function(v,i){%>
		<%if(!i){%>
		<div class="l">
			<a href="<%=v.basic_url%>">
				<img class="bg" src="<%=helpers.drawImage(v.basic_image_1)%>" />
				<%if(v.basic_image_2){%>
				<img class="abs" src="<%=helpers.drawImage(v.basic_image_2)%>" />
				<%}%>
			</a>
		</div>
		<ul class="r">
			<%}else{%>

			<li>
				<a href="<%=v.basic_url%>">
					<img class="bg" src="<%=helpers.drawImage(v.basic_image_1)%>" />
					<%if(v.basic_image_2){%>
					<img class="abs" src="<%=helpers.drawImage(v.basic_image_2)%>" />
					<%}%>
				</a>
			</li>
			<%}%>
			<%})%>
		</ul>
	</div>
	<%}%>
</div>

<%if(isReg == 1){%>
	<div class="channer-page_pop">
	<div class="mask"></div>
	<div class="mn">
		<p>代金卷已放到您的卡包啦，购买商品即可<br>使用哦！</p>
		<a onclick="_hmt.push(['_trackEvent', 'signcouper', 'register', '4'])" href="http://store.putao.com/">立即购买</a>
	</div>
	<a onclick="this.parentNode.hidden=true;_hmt.push(['_trackEvent', 'signcouper', 'register', '4'])" class="close" href="javascript:;"></a>
</div>
<%}%>
<script src="<%=helpers.staticurl('/js/lib/swiper2.js')%>"></script>
<script src="<%=helpers.staticurl('/js/index.js')%>"></script>
<script src="<%=helpers.staticurl('/js/lib/jquery1.42.min.js')%>"></script>
<script>
	(function() {
		var $flake = $('<div id="snowbox" />').css({
			'position': 'absolute',
			'top': '-50px',
			'z-index':10
		}).html('&#10052;');
		var documentHeight = $(document).height();
		var documentWidth = $(document).width();
		var defaults = {
			minSize: 20, //雪花的最小尺寸
			maxSize: 30, //雪花的最大尺寸
			newOn: 500, //雪花出现的频率
			flakeColor: "#FFFFFF"
		};
		var options = $.extend({}, defaults);
		var interval = setInterval(function() {
			var startPositionLeft = Math.random() * documentWidth - 100,
				startOpacity = 0.5 + Math.random(),
				sizeFlake = options.minSize + Math.random() * options.maxSize,
				endPositionTop = documentHeight - 40,
				endPositionLeft = startPositionLeft - 100 + Math.random() * 500,
				durationFall = documentHeight * 10 + Math.random() * 5000;
			$flake.clone().appendTo('body').css({
				left: startPositionLeft,
				opacity: startOpacity,
				'font-size': sizeFlake,
				color: options.flakeColor
			}).animate({
				top: endPositionTop,
				left: endPositionLeft,
				opacity: 0.2
			}, durationFall, 'linear', function() {
				$(this).remove()
			});
		}, options.newOn);
	})()
</script>
<% include ./footer.ejs %>